- name: include vs import 
  hosts: local
  connection: local
  roles: 
  - include-import



  #  import_roles parse the playbook before execution, it is static. But include_roles is dynamic it will directly execute the playbook, it won't parse the like import_role before execution.

